# Decisions — read this before any architectural choice

Resolved defaults from blueprint §2 (principles) and §17 (open questions). Do not re-litigate these mid-build; if one turns out wrong, change this file in a commit that says why.

## Principles (non-negotiable)
1. **Files in git are the source of truth; the console is a projection.** No lifecycle fact exists only in the app.
2. **Stage is derived, never stored.** `stage` is a pure function of accepted artifacts + eval verdict.
3. **A gate decision is a human-authored git commit** (a PR merge in GitHub mode). Agents have no code path to accept/merge/approve.
4. **Deterministic checks are code; judgment is human.** No auto-pass, no bypass button.
5. **Evidence is literal toolchain output**, displayed verbatim.
6. **Orchestration ≠ inference.** The console never calls a model; it launches and observes harnesses (Claude Code first) and records the model as a variable.
7. **The console parses repo config (`CLAUDE.md`, `.claude/**`, `REVIEW.md`, `bands.yaml`); it never edits it.**
8. **No approval prompts inside build sessions.** Approvals live at PR/production gates.
9. **AUTO mode is derived eligibility**; humans may only downgrade.
10. **Local-first, hosted-capable.** Everything works against a local clone with no network first.
11. **No database.** SQLite only as a disposable, rebuildable cache under `.sdlc-state/`.
12. **Core package has no I/O.** Pure functions over a `Tree` snapshot; adapters do I/O.

## Resolved ambiguities (blueprint §17)
| # | Question | Decision |
|---|---|---|
| Q1 | Spec §7 says auth/persistence/git are out of scope | Applies to the reference HTML only. Build on real git from Phase 0; defer auth to Phase 3. |
| Q2 | Loop = same change or new change? | Both. Gate-6 accept → same id, `cycle+1`, previous artifacts archived under `cycles/<n>/`. Triage/security accept → new change with `origin` link. |
| Q3 | Who is the gate-5 "engineer"? | Role `eng` owns it in the UI; the merge executes through branch protection, which decides code-ownership. |
| Q4 | Tech-lead consult at gate 2 for high-risk | One owner per gate. Record a `consult.tech_lead` event required before gate-2 accept when risk = high (Phase 2 rule). |
| Q5 | Where gate-1 acceptance is recorded | Always `gate.accepted{1}` in `log.jsonl`; local mode commits to default branch, GitHub mode merges the intent PR. |
| Q6 | Intent home | `sdlc/changes/<id>/` co-locates all six artifacts. `intentHome` config for a separate repo later. |
| Q7 | Session mode names | PLAN = harness plan mode; SUPERVISED = default with prompts; AUTO = auto-accept under managed permissions; HEADLESS = `claude -p`. |
| Q8 | Spec's `Change.docs` bodies, string timestamps, shared counter | UI seed model only. Bodies are files; ISO timestamps in front-matter; ids by directory scan on default branch + local branches. |
| Q9 | "Eval coverage exists for those paths" | An `active` eval case whose `paths[]` matches every planned file, OR verification includes a test target whose globs cover them (config `eligibility.coverage: strict|lenient`). |
| Q10 | Evidence size in git | Commit final-round output/screenshots under 1 MB per file (images ≤ 500 KB); larger → CI artifact with hash + URL. |
| Q11 | Who runs the headless design pass | CI job on intent merge; skills from repo or pinned plugin marketplace. |
| Q12 | Security findings source of truth | Scanner owns the finding; console owns routing status + dismissal reason in `sdlc/security/findings/*.yaml`, matched by scanner id. |
| Q13 | Metrics | Derived from real sources; "n/a · needs <source>" when missing. Seed numbers only in fixtures. |
| Q14 | Clock authority | Commit timestamps for gate `since`; session events carry harness wall time + per-session sequence. |
| Q15 | Two personas vs many responsibilities | Role switcher = view. Extra roles declared in `sdlc/config.yaml` and only gate specific non-gate actions / PR reviews. |

## Stack (fixed)
TypeScript, Node ≥ 22, pnpm monorepo. Packages: `schemas`, `core`, `adapters/git`, `adapters/github`, `server`, `cli`, `mcp`, `hooks`, `web`. React + Vite, plain CSS variables. Real `git` CLI via a thin wrapper. JSON Schema (Ajv) + zod types, `gray-matter` for front-matter. better-sqlite3 cache. MCP TypeScript SDK. Vitest.

## Decisions made during build
Choices the blueprint and the sections above did not settle, recorded as they were made. Each names the item that forced it.

| Item | Decision |
|---|---|
| 0.1 | ESM everywhere (`NodeNext`), `tsc -b` project references, tests import workspace packages from source via a Vitest alias. `packages/core` may depend only on `@sdlc/`-scoped packages; ESLint bans every Node builtin in `packages/core/src/**`. |
| 0.2 | zod is the authoring source of truth; JSON Schema 2020-12 is generated from it (`z.toJSONSchema`) and committed under `packages/schemas/json/` for MCP tool schemas and non-console writers; Ajv compiles the generated schema for runtime validation. A test fails when the committed JSON drifts from the zod source. |
| 0.2 | Two ledger invariants are encoded in the event schema itself, not only in the rule engine: `gate.accepted` and `gate.sent_back` require a human actor; `stage.entered` and `cycle.archived` require the system actor; agent actors require a `session`. |
| 0.2 | Stored enum spellings: `risk: routine|high`, `kind: feature|fix`, `sev: high|medium|low`, finding `status: new|patch_pr|escalated|dismissed`, triage `status: open|accepted|dismissed`. Display strings ("high risk", "patch in PR gate") are a UI concern. Session modes keep the Q7 upper-case names. |
| 0.2 | `change.yaml` carries `record`, `repro`, `closed` as required-but-nullable, matching blueprint §5.2. Threshold defaults live in `CONFIG_DEFAULTS` (schemas) and are applied by core, never written back to `config.yaml`. |
| 0.2 | `pr.yaml` has `provider: github|local` so local mode can record a branch merge without a code host; `number`/`url` are optional for that reason. |
| 0.2 | Schemas beyond the 0.2 list (`pr.yaml`, `deploy.yaml`, per-change run, final round, repro proof) were added now because Phase 1 writes those files and every `sdlc/` file must have a schema. |
| 0.3 | Parsers live in `@sdlc/schemas` (the blueprint's "schema / parser layer", §7.2) because they need `gray-matter` and `yaml`, which `packages/core` may not depend on. Front-matter YAML is parsed with the `yaml` core schema so timestamps stay strings. |
| 0.3 | `CLAUDE.md` "Verifying your work" line grammar: `- Build\|Test\|Lint\|Visual: \`cmd\` (healthy output)`, `- Test files: \`glob\`, …`, `- Max rounds: N`. A command chaining `&&`, `;` or `\|` is not single-target. "Under one page" = 600 words (`ONE_PAGE_WORDS`). |
| 0.3 | Artifact completeness: a required `##` section that is absent is an error (`artifact.section.missing`); one whose body is blank, `<placeholder>` or a bare list marker is a warning (`artifact.section.empty`). The gate-open rule (0.6) treats both as blocking; drafts stay inspectable. |
| 0.3 | `.claude/settings.json` hook rows: command hooks are `block` (exit 2 blocks); `permissions.deny/ask/allow` map to `block/ask/allow` rows so the "ask in edit/command phase" lint has something to see. Hook names come from `sdlc hook <name>` or the script basename. Phases for the four managed hooks are fixed by name. |
| 0.3 | `bands.yaml` shape: `metrics[]{metric, baseline, unit?, rules?, tiers{1sigma{action:log}, 2sigma{action:diagnose, tools[]}, 3sigma{action:propose, routes[]}}}`, plus `baselineWindow?` and `runbooks?` (the allowlist). |
| 0.3 | `bands.yaml` `tiers` keys are `1sigma`/`2sigma`/`3sigma` (ASCII) in the file; the UI renders σ. |
| 0.4 | Gate open/closed for gates 1, 2, 3, 6 is decided by the latest of `artifact.committed{idx}`, `plan.final`, `plan.drafted` (gate 3 only) and `gate.sent_back{gate}` in the current cycle: committed/final opens, drafted/sent-back closes. A present artifact with no events at all counts as open (hand-written fixtures). Gate 5 is open whenever `pr.yaml` exists unmerged; `since` = `pr.yaml.openedAt`. |
| 0.4 | `gate.since` uses the opening event's `ts` (the accepting commit writes the event, so it equals the commit time within a second); no commit-graph walk is needed in core. |
| 0.4 | Stage 4 evaluates only `sdlc/changes/<id>/evals/run-<n>.json`: a green run counts when its `configRef` matches the tree's `CLAUDE.md`, `.claude/settings.json` and `SKILL.md` blob shas. The model is recorded, never matched (it is not in the tree). Two consecutive red runs → `waiting on you`. |
| 0.4 | Derivation inconsistencies are validation errors that remove the change from every queue: gate accepted out of order, gate 6 accepted without `cycle` advancing, accepted gate whose artifact is missing, green evals with no `pr.yaml`, high-risk gate 3 accepted with `source ≠ pr.merge`. |
| 0.4 | Lenient eval coverage (Q9) holds when every planned path has an active case OR the verification block has a `test` command; strict requires the case. |
| 0.4 | `planMatches` follows the last `plan-sync` hook decision in the cycle (`hook.allowed` → true, `hook.blocked` → false), falling back to `pr.yaml.planMatches`, else null. |
| 0.4 | `products[]` nesting of `sdlc/` per product is not implemented in `loadRepo`; one `sdlc/` tree per repo until a monorepo needs it. |
| 0.5 | A `WritePlan` is `{changeId, files[{path, content|null}], events[{changeId, event}], commitMessage, trailers, actor}`. Events are appended by the adapter to that change's `log.jsonl`; deletes apply before writes. `applyWritePlan(tree)` gives tests and the filesystem adapter the same semantics the git adapter must implement. |
| 0.5 | Transitions take a `TransitionContext {now, newId, actor: HumanIdentity, mergeSha?, knownIds?}`. There is no agent-shaped context, so `accept`/`sendBack`/`loop`/`confirmTasks`/`confirmRepro` are unreachable by agents at the type level; ownership is checked against `sdlc/config.yaml` identities and refused when the config is missing. |
| 0.5 | `config.codeHost: local|github` (default local). High-risk gate 3 in local mode is accepted via CLI by an identity holding `tech_lead` (event `source: cli`, actor role `tech_lead`); in github mode only `source: pr.merge` is valid. |
| 0.5 | Gate 5 in local mode: the adapter merges the task branch first and passes `mergeSha`; the write-plan records `pr.yaml.mergedAt/mergeSha` and events `gate.accepted{5}`, `pr.merged`, `stage.entered{6}` in one commit. |
| 0.5 | `loop` archives every file of the closing cycle except `change.yaml`, `log.jsonl` and `cycles/` under `cycles/<n>/`, sets `kind: fix` and `repro: null`, seeds `intent.md` by mapping incident sections (Anomaly → Problem, Affected systems → Affected users and systems), and drafts `evals/cases/INC-<CHG>-<n>.json` with the closing cycle's plan paths. The ledger is one file across cycles; events carry `cycle`. |
| 0.5 | Task split proposal groups plan files by directory; any two tasks sharing a file merge into one `sequential` task. Worktree = branch = `<CHG>/<slug>`. |
| 0.5 | `stage.entered` is written by the same commit as the gate acceptance (system actor), so history is explicit without a separate engine pass in local mode. |
| 0.6 | The validation engine returns `RuleDiagnostic {…, blocking, changeId?}`; `severity: error` ⇒ blocking. `validateTree` (whole tree), `validateChange`, `validateDiff(before, after)` (risk/kind immutable at stage ≥3, archived cycles and ledger append-only, changes never deleted), `validateIds(idsByRef)` (an id on two non-default branches is a duplicate), `validateWritePlan` (every event re-validated, then the tree after the plan). |
| 0.6 → 0.7 | Amended in 0.7: an incomplete artifact keeps its gate **closed** (status "Agent producing … · draft incomplete (sections)", doc state `draft`), matching §11.1 "before a gate can open". The first real round-trip showed a template-created intent would otherwise fail `sdlc validate` while the agent is still writing it. A gate accepted on an incomplete artifact (only possible by hand-editing the ledger) is the blocking rule `gate.accepted-incomplete`. |
| 0.6 | A looped change (cycle > 1) is held at stage 4 by derivation until `evals/cases/INC-<CHG>-<n-1>.json` is `active`; status names the case. Past stage 4 with the case inactive is a blocking diagnostic. |
| 0.6 | The three managed hooks are pure functions in core (`check.planSync`, `check.testFreeze`, `check.verifyBeforeDone`) returning `{allowed, reason, offending}`; the hooks package (1.4) only adapts stdin/exit codes. plan-sync ignores `sdlc/changes/**` so ledger writes never trip it. |
| 0.7 | The git adapter reads only the console's prefixes (`sdlc`, `evals`, `.claude`, `CLAUDE.md`, `REVIEW.md`, `bands.yaml`, `.gitattributes`) via `ls-tree` + `cat-file --batch`; binary files keep their sha with empty content. `readWorkingTree` computes git-compatible blob shas so uncommitted state derives identically. |
| 0.7 | `commitWritePlan` writes the plan's files, appends events, then `git add`/`commit --only` exactly those paths, so unrelated dirty files never ride along. Author = committer = the acting identity; trailers are appended to the message body. Gate 5 in local mode uses `mergeBranch` (`--no-ff`) and passes the merge sha into `accept`. |
| 0.7 | Ledger union = `git show <branch>:sdlc/changes/<id>/log.jsonl` over every local branch, deduplicated by event id, sorted by seq. ULIDs are generated in the adapter (`newUlid`), never in core. |
| 0.7 | A change whose current-stage artifact is incomplete shows "Loop closed — re-entered Plan from incident" when it has just looped, otherwise "Agent producing … · draft incomplete (sections)". |
| 0.8 | CLI argument parsing uses `node:util.parseArgs` (no CLI framework in the fixed stack). `main(argv, io)` takes an `Io` so tests drive it in-process; the bin wraps `process`. File arguments resolve against the invocation directory. |
| 0.8 | `sdlc init` writes `sdlc/config.yaml` (git identity holds po+eng+tech_lead), the four templates, queue `.gitkeep`s, `.gitattributes` merge=union and `.sdlc-state/` in `.gitignore`; it never overwrites and never touches `.claude/**` (hook installation is 1.4 and is also create-only). |
| 0.8 | Identity: `SDLC_IDENTITY` (email) overrides `git config user.email`; `SDLC_ACTOR_TYPE=agent` makes every mutating command exit 2 before touching the repo. Exit codes: 0 ok, 1 error or blocking validation, 2 refused. |
| 0.8 | `sdlc loop --incident <file>` makes two commits: the incident record (`artifact.committed{5}` by the human) then the gate-6 write-plan, so the decision commit stays a single event. |
| 0.8 | `sdlc audit` breaks the chain on: blocking validation diagnostics, a gate decision whose actor is not human or lacks the role, a decision with no commit carrying its `SDLC-Event` trailer, a trailer actor ≠ event actor, a commit naming an event absent from the ledger, an agent-produced artifact without `context_manifest`, or spec/plan shas that do not chain. |
| 0.9 | `@sdlc/fixtures` lives at `fixtures/` (a workspace member, not under `packages/`) so "fixture repos under fixtures/" stays literally true. `fixtures/seed/` is generated from `seedFiles()` and a sync test fails on drift; every ledger sha is the real git blob sha of the file so chaining holds in a real repo. |
| 0.9 | Seed ids follow the 4-digit convention: CHG-0012/0017/0018/0019/0020/0021/0022/0023, TRI-0042/0043, SEC-0118/0119/0120, PRP-0007. Sessions are seeded from `fixtures/sessions.seed.json` (runtime cache data, outside the repo tree). |
| 0.9 | Triage/finding routing are core write-plans: `acceptTriage` (new change with `origin: triage:<id>`, item deleted in the same commit), `dismissTriage` (status + reason kept as history), `patchFinding`, `escalateFinding` (new high-risk fix change + `CASE-<SEC>` draft eval case + `escalatedTo`), `dismissFinding`. Triage actions need `po` or `service_owner`; finding actions need `eng` or `security`. `WritePlan.changeId` is null for plans that touch no change. |
| 0.9 | Agent-produced artifacts carry `context_manifest`; the seed sets it, and `sdlc audit` reports its absence as a break. Seed decisions were committed in one baseline commit, so their per-decision `SDLC-Event` trailers do not exist — the real-git fixture test tolerates only that class of break. |
| 1.1 | `@sdlc/server` uses `node:http` + `ws` (no web framework in the fixed stack). The snapshot is derived from the tree at HEAD (files in git are the truth); the working tree is not watched for derivation. |
| 1.1 | Transport sends full snapshots with a monotonically increasing `revision` on connect and after every refresh; incremental patches are deferred until snapshots get large. The UI treats each message as a replacement. |
| 1.1 | Refresh triggers: any HTTP action (after its commit) and a debounced `fs.watch` over `.git/HEAD`, `.git/refs`, `.git/packed-refs`, `sdlc`, `evals`, `.claude`, `CLAUDE.md`, `REVIEW.md`, `bands.yaml`; a refresh is a no-op when HEAD did not move. |
| 1.1 | HTTP errors: 400 bad input, 403 when the first diagnostic is an ownership rule (`*.not-owner`, `*.not-engineer`, `gate.via-*`), 404 unknown id, 409 any other precondition or blocking validation (body carries diagnostics), 502 git/merge failure with `retryable: true`. Toast text is chosen by the server from the real outcome. |
| 1.1 | Sessions are supplied to the server by a provider function (`sessions()`); 1.1 has no registry of its own — 1.6 adds the SQLite-backed registry. No SQLite is needed before then. |
| 1.2 | `@sdlc/web` is React 19 + Vite 8 with plain CSS variables (`tokens.css` carries every spec §6 token; `kilnpulse` is the only animation). TypeScript uses `moduleResolution: bundler` and the DOM lib for this package only; `build` = `tsc --noEmit && vite build`, and the root `pnpm build` runs it after `tsc -b`. |
| 1.2 | Fonts are font-family stacks ("Source Sans 3", "JetBrains Mono" with system fallbacks); no network font links, keeping the console local-first. |
| 1.2 | Web tests render through `react-dom/server` in Vitest's node environment (no DOM shim, no testing library); the reducer holds all interaction rules from spec §2/§5 and is unit-tested. |
| 1.2 | The client holds `UIState` only and treats every WebSocket message as a full replacement snapshot; toasts come from the server response. Role switching is disabled for roles the identity does not hold (tooltip explains). |
| 1.2 | `sdlc serve` defaults to port 7331 and serves `packages/web/dist` at `/` when the console is built (unknown paths fall back to index.html; there is no router). |
| 1.3 | Metrics are a pure core function over the Repo and ledger (`computeMetrics`): a fixed catalogue of leading/lagging values per stage, each declaring its unit and which direction is better; trend compares the 30-day window with the previous one (±5% = flat) and is null without data. Metrics needing PR metadata, CI, eval-case history or detection snapshots render `n/a · needs <source>` (Q13). |
| 1.3 | Findings ingest (`sdlc security import <csv|md>` / `POST /api/findings/import`) allocates `SEC-NNNN` for unknown scanner ids and updates known ones in place, so a dismissed finding never comes back as `new` (matched by scanner id, Q12). Import needs `eng` or `security`. |
| 1.3 | Bands table CURRENT/TIER/STATUS show "no data · needs detection snapshots" until the Maintain automation (Phase 3) writes snapshots; the table and tier footer render from `bands.yaml` alone. |
| 1.3 | Dismissals in the web use a text prompt for the required reason; an empty answer cancels the action rather than sending an empty reason to the server. |
| 1.4 | Hooks are thin adapters in `@sdlc/hooks` over `check.*` in core, invoked as `sdlc hook <name>` with Claude Code's stdin JSON; exit 2 blocks and the reason goes to stderr. Internal errors fail open (exit 0, note on stderr) so a broken hook never freezes a session; running outside a `CHG-NNNN/*` branch (or without `SDLC_CHANGE`) allows silently. |
| 1.4 | Hook decisions are appended to the working-tree ledger of the change as agent events (`hook.blocked`, `hook.allowed` for plan-sync, `round`), so they ride along with the next commit and union-merge across branches. |
| 1.4 | verify-before-done runs the CLAUDE.md verification commands itself on Stop, records the round under `.sdlc-state/sessions/<session>/rounds.jsonl` (verbatim tails) and in the ledger, and blocks unless the round is all green with output. A repo without a verification block cannot report done. `stop_hook_active` short-circuits to avoid loops. |
| 1.4 | `sdlc init` installs `.claude/hooks/*.sh` wrappers and a `.claude/settings.json` only when absent; an existing settings.json is never edited — the snippet to add is printed. Wrappers prefer `sdlc` on PATH and fall back to the bin path recorded at init (`SDLC_BIN` overrides). |
| 1.5 | `sdlc-mcp` exposes exactly nine tools over stdio: `list_work`, `get_change`, `get_context`, `propose_artifact`, `submit_plan_revision`, `report_round`, `report_done`, `request_input`, `log_note`. No tool accepts, merges, approves, lifts a freeze, confirms a repro or edits risk/kind (§8.5, §9.3). `propose_tasks`, `report_verifier`, `propose_test_change`, `propose_claude_md_line`, `create_triage_item` are Phase 2. |
| 1.5 | Agent writes are commits on the current *change* branch (`sdlc/<CHG>/<artifact>` or `<CHG>/<task>`) under the agent identity `claude-code <claude-code@sdlc.local>` (`SDLC_AGENT_ID` overrides); writes on the default branch are refused. In local mode the gate owner's accept merges `sdlc/<CHG>/<artifact>` into the default branch first (server and CLI), mirroring gate 5. |
| 1.5 | `propose_artifact` completes and chains the front-matter itself (author/created, `intent_sha`/`spec_sha` from the accepted gate's `artifactSha`, `context_manifest` from the bundle hash) and commits only complete artifacts; incomplete drafts come back as diagnostics. `submit_plan_revision` increments `rev` and `final=true` opens gate 3. |
| 1.5 | Rounds live in `.sdlc-state/sessions/<session>/rounds.jsonl` (shared with the verify-before-done hook) plus an uncommitted `round` event in the ledger; `report_done` applies `check.verifyBeforeDone` and, when green, commits `evals/final-round.json` with `session.stopped`, sweeping the pending round events into the same commit. Flaky = red→green with an unchanged code diff (ledger and `.sdlc-state` excluded from the fingerprint). |
| 1.5 | `sdlc init` writes `.mcp.json` (create-only) pointing Claude Code at `node <bin> mcp`; the context bundle's `allowedTools` names the MCP tools as `mcp__sdlc__<tool>`. |
| 1.6 | Session registry = better-sqlite3 at `.sdlc-state/sessions.db` (disposable, rebuildable). A session's own files (`rounds.jsonl`, `waiting.json`, `mcp.json`, `prompt.md`, `context.json`, `stream.jsonl`) live under the *worktree's* `.sdlc-state/sessions/<id>/`, where hooks and the MCP server also write. |
| 1.6 | Worktrees: `.sdlc-state/worktrees/<branch with / → __>` (gitignored, inside the repo); artifact sessions branch `sdlc/<CHG>/<artifact>`, build sessions `<CHG>/<task>`. |
| 1.6 | Launcher runs Claude Code headless: `claude -p <prompt> --output-format stream-json --verbose --permission-mode plan|acceptEdits --mcp-config <session mcp.json> --allowedTools <bundle> --session-id <uuid>` with `SDLC_SESSION`, `SDLC_CHANGE`, `SDLC_ACTOR_TYPE=agent` in the environment. PLAN → `plan`, AUTO/HEADLESS → `acceptEdits`. SUPERVISED sessions are prepared (worktree, config, prompt, `session.started`) and the command is handed to the engineer instead of spawned. `SDLC_CLAUDE_BIN` overrides the executable (tests use a fake). |
| 1.6 | Preconditions (FR-32/34/35): a build session needs a target (prefilled from the task or the plan's acceptance line; otherwise "waiting on you: define done"); AUTO only when `autoEligible`; refuse when done-but-unreviewed sessions exceed `thresholds.sessionCeiling`. Add guidance = relaunch with `claude --resume <harness session>` and the guidance as the prompt (headless runs cannot take input mid-run). Raise cap once adds 5 rounds. |
| 1.6 | `session.started` and (unless `report_done` already recorded it) `session.stopped` are committed on the session branch by `sdlc-bot`; the observer tees stream-json to `stream.jsonl` and records `modelPin`, cost, turns and errors in the registry. |
| 1.7 | Lifecycle records written by the engine (`evals/run-<n>.json`, `evals.green|red`, `pr.yaml`, `pr.opened`, `stage.entered{5}`) are committed on the **default branch** by `sdlc-bot`; code stays on the task branch until the gate-5 merge. Derivation therefore sees stage 5 without merging code, and the run's `headSha` names the task-branch head it tested. The run's file set is the code diff `base...branch` minus `sdlc/changes/**`. |
| 1.7 | Per-change run = CLAUDE.md verification commands (exit code decides; `healthyOutput` there is descriptive) + checks of active eval cases whose `paths[]` intersect the diff (an eval check's `healthyOutput` is a literal that must appear). No commands ⇒ never green. |
| 1.7 | Engine triggers are keyed jobs in the same SQLite file (`<change>:<cycle>:<stage>:<artifact sha>[:kind]`; runs keyed by session, resume count and worktree head): design pass after gate 1, plan session after gate 2, task split + build session after gate 3 (AUTO when eligible, else prepared SUPERVISED), per-change run on build-session exit, PR on green, one automatic resume with the failing output on the first red, waiting-on-you on the second. The engine is opt-in (`sdlc serve --engine`); without it every step is available manually (`sdlc session start`, `sdlc run`, `POST /api/changes/:id/run`). |
| 1.7 | Code host: `LocalCodeHost` is complete (local PR record + git merge). `GitHubCodeHost` exists only as an interface that refuses without credentials; GitHub mode needs a token and is Phase 2. |
| 1.8 | Config is read-only except proposal dismissal (reason required; `eng` or `platform`). "Accept proposal" and "Run suite" are disabled with their Phase 2 reason (code-host PR, CI adapter) rather than hidden. Skill pass % shows "n/a · needs trigger tests" until the trigger-test runner exists. |
| 1.8 | The web bundle mirrors `ONE_PAGE_WORDS` locally instead of importing `@sdlc/schemas` at runtime, keeping Node-only code out of the browser bundle. |
| 1.6 → e2e | Amended after the first real run: Claude Code's `plan` permission mode denies MCP tool calls (`get_context`, `submit_plan_revision` were refused), so plan sessions run with `--permission-mode default` and the bundle's read-only `allowedTools` (Read, Grep, Glob + the MCP tools) is what keeps them from editing. The engine now closes the job that launched a session when the session exits. |
| 1.4 → e2e | Hooks take the console session id from `SDLC_SESSION` (set by the launcher) and fall back to the harness's own `session_id`, so hook decisions and rounds share the session record with the MCP tools' writes. Ledger `seq` is monotonic per branch; parallel branches can reuse numbers and the union merge orders ties by timestamp and id. |
| 2.1 | GitHub mode runs on a token, not a GitHub App: `@sdlc/adapter-github` is a thin REST client over the global `fetch` (no octokit). Credentials and addressing come from the same variables GitHub Actions uses — `GITHUB_TOKEN` (or `GH_TOKEN`), `GITHUB_API_URL` (enterprise or a test double), `GITHUB_REPOSITORY=owner/repo` (otherwise parsed from the `origin` remote). Pushes use the operator's git credentials; the token never appears in a URL. |
| 2.1 | Under a token the "check run" is a commit status (`sdlc/evidence`; later `sdlc/findings`, `sdlc/evals`): check runs need an App, and branch protection can require statuses just as well. The App and its "on behalf of" authorship stay with hosted mode (Phase 3). |
| 2.1 | The adapter refuses to push or merge when the base branch reports `protected: false`. GitHub mode only exists under branch protection; an unprotected base would let the console's merge walk around the gate, so the answer is a non-retryable refusal, never a fallback to a local merge. Likewise `codeHost: github` without a token refuses instead of degrading to local mode. |
| 2.1 | Gate 5 in GitHub mode: the console (or `sdlc accept`) merges the PR through the API as the acting human with `sha` = the tested head (409 when the branch moved, 405 when protection blocks; both surface as retryable 502s), then fetches `origin/<base>` into the local base branch and records `gate.accepted{5, source: pr.merge}` + `pr.merged{mergeSha}`. `TransitionContext.source: "pr.merge"` is the only source that satisfies a `via_branch_protection` / `via_pr` gate; derivation flags a github-mode gate 5 recorded with any other source. |
| 2.1 | The `CodeHost` contract (`openPr`, `merge`) and `LocalCodeHost` live in `@sdlc/adapter-git`; the server only picks a host from `config.codeHost` and the environment. Both hosts write the same `pr.yaml` mirror and ledger events. |
| 2.1 | Lifecycle records (`evals/run-<n>.json`, `pr.yaml`, ledger events) still commit on the console's local default branch in GitHub mode; branch protection means they cannot be pushed directly. Until item 2.2 defines the ledger sync, the local clone is the console's source of truth and origin holds the code PRs and merge commits. |
| 2.1 | CODEOWNERS is parsed with gitignore anchoring (a slash anywhere but the end anchors to the root; last matching rule wins) from `CODEOWNERS`, `.github/CODEOWNERS` or `docs/CODEOWNERS`, in that order. It informs ownership display and the 2.2 high-risk routing; the console never edits it. |
| 2.2 | The console's snapshot is the committed default branch with every unmerged `sdlc/<CHG>/<artifact>` branch's change directory laid over it and the ledgers unioned (`readTreeWithBranches`). A draft in review is therefore visible as `pending-review` in both modes before its accept merges it; files outside the change directory always come from the default branch, and the CLI's committed-tree reads are unchanged. |
| 2.2 | In GitHub mode every unmerged artifact branch becomes a pull request opened by the engine (or `sdlc sync`), recorded as `pr.opened{artifact, branch, number, url, headSha}` on the branch itself so the record merges with the artifact. `ChangeView.artifactPrs` derives from those events; merged = the gate they carry is accepted. |
| 2.2 | Accepting a gate whose artifact is a PR commits `gate.accepted{source: pr.merge}` (plus `stage.entered`, plan front-matter, the loop archive for gate 6) on the PR branch, pushes, and merges the PR through the API with the branch head as precondition; the decision and the artifact reach the default branch in one merge under branch protection. Send-back commits `gate.sent_back` on the branch and posts a "request changes" review with the feedback; the agent's next draft reopens the gate on the same PR. |
| 2.2 | An artifact whose file already sits on the default branch (a seed, a looped cycle's intent, a PR merged on GitHub before its decision was recorded) is accepted in the console with `source: console`; the decision reaches origin through the records PR. |
| 2.2 | Merges done on GitHub itself are detected by polling the recorded PRs (every 30 s on engine ticks, or on demand) — the webhook receiver is item 2.4. The decision is recorded under the identity whose `github` login (new optional field on config identities) matches the merger; an unmapped merger is recorded as `<login>@users.noreply.github.com` only if that passes the gate-ownership rule, otherwise nothing is written and the sync reports why. The console never guesses who decided. |
| 2.2 | Lifecycle commits the console makes on its local default branch reach origin through one long-lived `sdlc/records` pull request that the system pushes and keeps current and a human merges; the system never merges anything into the default branch. Artifact branches are cut from the local default branch, so an artifact PR may carry earlier local records too — git shares the commits, nothing is duplicated. When origin moves (records PR merged, PR merged on GitHub) the local default branch takes it with a merge commit before pushing. |
| 2.2 | High-risk gate 3 in GitHub mode is exactly the merge-detection path: the console shows the plan PR and offers only Send back; the tech lead merges on GitHub (code-owner review enforced by branch protection) and the console records `gate.accepted{3, source: pr.merge}` under the mapped identity. |
| 2.3 | The review job is a headless Claude Code session of kind `review` (one per change, cycle and PR head; key `<CHG>:<cycle>:5:review:<head>`) that reads the PR branch worktree with read-only tools and REVIEW.md verbatim as its policy. It reports through `mcp__sdlc__report_finding`, which keeps findings beside the session's rounds (`.sdlc-state/sessions/<id>/findings.jsonl`) and commits nothing: a commit on the PR branch would move the head past the tested run and break the merge precondition. The session's own `session.*` lines go to the default branch for the same reason. |
| 2.3 | When the review session ends, the system mirrors it (`mirrorReview`, sdlc-bot, default branch): one `review.finding` event per finding with the agent and session as actor, and on `pr.yaml` the severity tally, a `findings` check (fail while a high finding stands) and `review {session, headSha, at}`. The reviewed head lives in git, so an engine restart never relaunches a review of the same head; a review of a head the run never tested is refused (`review.head-mismatch`), and the per-change run must run again. |
| 2.3 | Checks are a list the run hands to the code host (`OpenPrInput.checks`): `evidence` (verification commands) and `evals` (intersecting eval cases; zero cases is a pass that says so). GitHub mode publishes each as a commit status `sdlc/<name>` on the PR head at open and `sdlc/findings` after the review; the findings themselves are posted verbatim as a `COMMENT` review — the adapter has no `APPROVE` path. |
| 2.3 | Findings inform; they do not block the console's merge. The code owner reads them on the PR and in the Change detail PR panel and decides; a team that wants a high finding to block makes `sdlc/findings` a required status in branch protection, which is theirs to set. The test-freeze fallback auto-finding that does block is item 2.7. |
| 2.3 | `ChangeView.findings` derives from the cycle's `review.finding` events after the code PR opened, most severe first; `Repo.reviewPolicy` is REVIEW.md's text and sha, parsed for the prompt and the bundle, never edited. |
| 2.4 | Inbound webhooks arrive at `POST /api/webhooks/github` on `sdlc serve`, accepted only with a valid `X-Hub-Signature-256` over the raw body under `GITHUB_WEBHOOK_SECRET` (constant-time HMAC); without the secret the route answers 503 and never reads a body as an event. A payload is data: the parser reduces it to routing facts (PR number, head, branch, ref) and the engine fetches and re-derives from git — the merger's identity comes from the API, never from the body. The server binds 127.0.0.1 by default; `sdlc serve --host` or a tunnel exposes the receiver, which is the operator's choice. |
| 2.4 | Idempotency is layered: a delivery id is recorded in the disposable cache so a redelivery is a no-op (`replay: true`), and every job a delivery can start is keyed on the head it concerns (run per `<session>:<head>`, review per `<CHG>:<cycle>:5:review:<head>`, gate recorded once) so a re-sent event under a new id finds its work claimed. A delivery the engine cannot act on answers 500 and is not recorded, so GitHub's redelivery reprocesses it. |
| 2.4 | Polling is the fallback, not switched off: the engine keeps polling the recorded PRs every 30 s until deliveries arrive, then backs off to every 10 min (`webhookQuietMs`) while they keep arriving. A merge the webhook missed is therefore recorded within one poll either way. Sync passes queue behind an in-flight pass instead of being dropped, because the merge a delivery reports may have landed after the running pass looked. |
| 2.4 | `pull_request.synchronize` on the code PR is the head-moved path: the engine fetches the new head, fast-forwards the branch worktree (diverged history is refused — the console never rewrites a branch), and runs the per-change run on it. Green records `pr.synchronized{number, headSha}` and the new head with the run's checks on `pr.yaml`, dropping the old head's tally and review so the review job relaunches and the findings cut-off moves; GitHub gets the checks as statuses on the new head, never a second PR. Red leaves `pr.yaml` at the old tested head, so the merge precondition refuses at GitHub (409, retryable) rather than merging an untested head. A build session still running on the branch owns the next run; the delivery is noted and skipped. The same path serves local mode: a manual `sdlc run` after new commits synchronizes the local PR instead of re-opening it. |
| 2.4 | The other deliveries route to what already exists: a merged PR (code or artifact) → the GitHub sync pass records the gate under the mapped identity → stage 6 (or 2/3/…); `push` to the default branch → the sync pass takes origin; `push`/`synchronize` on an `sdlc/<CHG>/<artifact>` branch → fetch and refresh; PR opened/reopened → sync (artifact PRs get recorded). Reviews, check runs and statuses are noted with the delivery and change nothing: gate decisions are recorded from merges only, and CI verdicts reach the console through committed run files (2.5). |
| 2.5 | The suite runner (`runSuite`, server; `sdlc evals run`) executes every `active` case's checks in a detached worktree at the ref, keeps output verbatim, and writes `evals/runs/RUN-NNNN.json` committed on the current branch by sdlc-bot — the console commits on the default branch, CI on the PR branch. Draft and retired cases never run and never enter the pass rate. Verdict is `pass` only when every active case ran and the rate meets `evals.threshold`; a run stopped by the budget is `incomplete` and counts for nothing (gate, metrics, signals). An active suite of zero cases passes vacuously — the under-sized warning is the honest signal there, not a fake failure. A run file claiming `pass` below its own threshold is a blocking validation error. |
| 2.5 | Cost has no currency for shell checks: the built-in runner records elapsed minutes in `EvalRun.cost` and `evals.budget` is read in the same unit as a rolling 30-day sum; a team wiring a paid runner records its own cost in the same field. Over the budget the runner refuses to start (`budget exhausted`, exit 2 / 409) and a run that crosses it mid-way stops and is `incomplete`. |
| 2.5 | Config-change gate = `sdlc evals gate` (pure `evalGate`): the latest run whose fingerprint matches the tree's CLAUDE.md, hooks and skills must be `pass`; below threshold (or incomplete, or no run for this config) exits 1 and lists regressed cases against the last run under a different config with before/after output verbatim. `sdlc init` writes `.github/workflows/sdlc-evals.yml` (PR paths CLAUDE.md, .claude/**, sdlc/config.yaml, evals/cases/**; schedule; dispatch) and `sdlc-validate.yml`, create-only; the workflow keeps the run file on the PR branch and, for scheduled/manual runs, on a long-lived `sdlc/evals-runs` PR since the default branch is protected. `evals.mode: scheduled` disables the config-pr trigger (`skipped`) and the gate reports "not gated"; the banner turns amber. No new console gate: the approver is the team owning the config, through branch protection. |
| 2.5 | Harvest (`harvestCase`, "Add as eval", `sdlc evals harvest`): a change at stage 6 becomes a `draft` case for the first identity holding `platform` (else the actor) — prompt from the title and acceptance line plus "behaviour outside this change is unchanged", checks from the verification commands, paths from the plan, `source {change, CHG}`; refused before the merge and when a case for the change exists. The change's ledger gets a `note`; `ChangeView.harvested` shows the case and its status. The incident loop's `INC-` draft (0.5) is the other harvest. |
| 2.5 | Live-suite signals are pure (`evalSignals`) and raised by whoever ran the suite (`raiseEvalSignals`, system actor): an active case passing `noDiscriminationRuns` consecutive complete runs → triage tier `eval-retire` ("retire or harden"); failing `brokenCheckRuns` consecutive complete runs under one fingerprint → tier `flaky` ("broken check"). Items carry `src: eval-retire:<CASE>` / `eval-broken:<CASE>` and a pre-drafted intent; a streak raises one item however many runs extend it (dedupe: an open item with that src, or one raised at or after the streak's first run). Retiring stays a human decision on the case file. |
| 2.6 | Capacity (FR-35) has one definition, `sessionCapacity` in the server, and every surface reads it: the snapshot's `capacity {active, backlog, ceiling, over}`, the launcher's refusal, the engine's hold and the Sessions header. Backlog = sessions done and awaiting review: a build or review session until a green run or mirror covered it (`reviewed`), an artifact session until the gate its artifact opened is decided, none once the change merges. Over the ceiling the engine claims no build-session job (a hold, logged once, not a failed job), so the tick after the backlog clears launches it. `thresholds.sessionCeiling: null` means no ceiling and the header shows counts only; the default stays 4. |
| 2.6 | The verification term of AUTO eligibility (`verificationTerm`) holds only for a loop the session can run: every command single-target ("chains commands — wrap in one target"), a test target, and — when the plan touches UI paths (`uiPaths`: web/ui/component/view/page directories or tsx/jsx/vue/svelte/css/html files) — a visual tool from a `Visual:` line. Lenient coverage needs test globs as well as a test target; path-level coverage by tests is not computable from a plan whose test files are new, so strict mode (an active case per path) stays the path-level guarantee. Eligibility recomputes live; a running AUTO session is not interrupted, its card says "no longer AUTO-eligible" and offers the downgrade. |
| 2.6 | AUTO → SUPERVISED is `overrideMode`: an `override.mode{from, to, reason}` event by an engineer, downward on the autonomy ladder PLAN < SUPERVISED < HEADLESS < AUTO only (`override.upward` refused by the transition and blocking in validation). The server's `downgradeSession` commits it on the default branch, parks the record as SUPERVISED / awaiting the engineer with a `--resume <harness session>` command for the same worktree, then ends the headless harness (the observer keeps the parked status; the task branch records `session.stopped{taken_over}`; the launching job closes as done, "downgraded"). Nothing grants AUTO: no route, no CLI flag, no event. |
| 2.6 | Visual rounds (FR-33): a round's `screenshotRef` is a path relative to the session worktree and `diffPct` is the session's own estimate against the mock — the console shows both and says who reported the diff; it does not measure pixels. `GET /api/sessions/:id/rounds/:n/screenshot` serves the file only from inside the worktree, `GET /api/changes/:id/design/:file` serves the mock's bytes from git (read-only). `ChangeView.visual {uiPaths, tool, mock, warning}` derives the first image under `design/` and the "UI work without a visual check" warning; the build prompt names the mock and asks for a screenshot and diff per UI round; the context bundle lists `design/*`. |
| 2.7 | Repro-first is a conversation on the ledger: the build session reports its failing test with `report_repro`, which commits that one file alone on the task branch (verified with `diff-tree`; a stray edit stays uncommitted), appends `repro.failed` and keeps the draft (path, reason, sha, verbatim output) beside the session — no tool confirms anything. The engineer's verdict is `repro.confirmed` (`confirmRepro`, after the console re-verifies the commit by sha through the shared object store) or `repro.rejected{testPath, reason}` (`rejectRepro`, new event); either way the session is resumed with the decision as guidance, and the engine runs no per-change run while a draft awaits — a red run would only resume the agent into fixing before anyone decided. `ChangeView.reproRejection` holds the last verdict until a newer `repro.failed` answers it. |
| 2.7 | Freeze lift is `liftFreeze`: one `freeze.lifted{path, reason}` per file per change by an engineer, a second refused (`freeze.already-lifted`) and a duplicate on the ledger a blocking validation error (`freeze.lifted-twice`). Console decisions live on the default branch, so the hook context merges the default branch's ledger for the change (and its committed repro block) into the worktree's before `check.testFreeze` runs — a lift granted in the console is honoured in the worktree it was granted for. |
| 2.7 | The per-change run carries the repro proof as a `repro` check with a literal summary ("repro test <path> committed <sha7> before fix · unchanged in diff · passing now"): fail when the repro test changed after its commit without a lift, or when the commit is not in the clone (the seed's placeholder sha reads that way rather than pretending). `pr.yaml` checks now keep their `summary`. With managed hooks installed nothing more is raised — the hook's blocks are on the ledger and a human edit is the human's. Without them the run raises one `test-freeze` auto-finding per test file changed after the repro commit without a lift ("diff touches a test file during a fix") plus a failing `test-freeze` check; dismissals are carried across a head move for paths still flagged. |
| 2.7 | The console's merge (gate 5 through console or CLI) refuses while an auto-finding is undismissed (`merge.auto-finding`) or a fix's repro check is not `pass` (`merge.repro-red`), checked before any branch moves; `dismissAutoFinding` needs a reason from an engineer and leaves a `note` on the ledger. A merge already done on the code host (`source: pr.merge`) is recorded as it happened — the console cannot un-merge, and a team that wants the same block on GitHub makes `sdlc/repro` / `sdlc/test-freeze` required statuses there. |
| 2.8 | The repeat-mistake signal is pure (`repeatSignals`): `hook.blocked` reasons and `gate.sent_back` feedback across every change's ledger, normalised (lowercase, trimmed, one space) and grouped per repository; a reason counts once per agent session for hook blocks and once per send-back decision, and two distinct sources make a signal. A proposal answers a reason through its new `reason` field, so a third occurrence counts onto it (`seen`) and files nothing new — the transition (`fileProposal`), the MCP tool and the engine's job key (`proposal:<reason key>`) all refuse a second proposal for the same reason, dismissed ones included: a dismissal is a decision, not a reset. |
| 2.8 | The proposal job is a headless session of kind `propose` (job `claude-md-proposal`) on the newest cited change that is still open: read-only tools plus `propose_claude_md_line`, prompt = the cluster verbatim, CLAUDE.md verbatim and the one-page word budget, branch `sdlc/propose/<CHG>` cut from the default branch with nothing committed on it, ledger lines on the default branch (as reviews). The tool keeps the draft beside the session; when the session ends the system files `sdlc/proposals/PRP-NNNN.yaml` (open, sdlc-bot, with a note on the cited change under the agent's name). No model is called by the console and no agent writes CLAUDE.md. |
| 2.8 | Accept (eng or platform) commits the line on `sdlc/proposals/<PRP>` under the accepting human's identity — appended to the first bullet list before any `##` heading, or at the end — then in GitHub mode pushes and opens the PR for the code owners (local mode records the branch); the proposal file turns `accepted` with `pr {branch, number?, url?}`. The console never writes CLAUDE.md on the default branch and never merges the PR: the "merged" chip is derived from the default branch carrying the line (`proposalLanded`), the "pending review" chip from its absence. A retried accept after a failed PR call finds the commit and reuses it. |
| 2.8 | A trigger test is an eval case with `skill: <name>`; its pass feeds the skill's pass % (share passed among the active trigger tests in the latest complete suite run that covers them) and, below `thresholds.skillPassThreshold` (default 0.8), a `skill-trigger` triage item ("skill not triggering") raised with the other live-suite signals. `sdlc evals trigger <skill> --prompt <text>` is the check command such a case uses: it runs the harness headless and exits 0 only when the stream shows the `Skill` tool invoked with that name — the console reads transcripts, it does not judge prose. Backed-by resolves against `.claude/settings.json`: a named hook that is not installed is flagged (`skill.backed-by.unknown`) and counts as advisory; findings citing a skill are the review findings naming it. |
| 2.9 | Records mode (FR-16) is derived, never stored: `requiredWritebacks` reads each change's ledger and, for every artifact whose `records.<artifact>` is `external` or `linked`, owes one write-back per `artifact.committed` (kind `committed`) and per `gate.accepted` (kind `accepted`) that has no `record.writeback.ok` for the same artifact, kind and sha; the two write-back events carry `artifact`, `kind`, `sha` (and `url`) for that match, and "synced" in the viewer header is the time of the latest ok. `linked` on `evals`/`pr` has no commit to write back and behaves as `external` (validation warns). A change with no record owes nothing — linking (`linkRecord`: po, eng or platform; `record.linked` + `change.yaml.record`, once) is what starts the write-backs. |
| 2.9 | The connector is the platform team's MCP server named by `records.connector` and defined in the project's `.mcp.json` (`mcpServers.<name>`, the file Claude Code reads; parsed, never edited): tools `record_get {system, id}` and `record_write_back {system, id, changeId, title, artifact, artifactName, kind, sha, by, at, url?}`. The console calls it as an MCP client over stdio, one process per call; a tool error or a process that does not start is a `ConnectorError`. Linking looks the record up first when a connector is configured, so a typo never becomes a link. |
| 2.9 | A write-back is an engine job keyed on the fact (`writeback:<CHG>:<artifact>:<kind>:<sha7>`, kind `record-writeback`): up to 3 attempts with doubling backoff, then `record.writeback.ok` or, on the first failure of that fact, `record.writeback.failed` by sdlc-bot on the default branch — the accept it followed stands. Failed write-backs are retried on ticks every `writebackRetryMs` (default 10 min) silently until they land, and at once through `POST /api/changes/:id/records/retry` / `sdlc record retry` (200 with the ok commit, 502 retryable while the connector keeps failing, no second failure event). Without a connector the failure names the missing config and `records.connector-missing` warns. |
| 2.9 | Linked mode blocks the human, not the agent: `accept` refuses `gate.linked.record-missing` without a record and `gate.linked.sha-not-written` until the artifact's current commit sha carries a `record.writeback.ok{committed}`; `ChangeView.recordBlock` says which (naming the failed write-back's error when there is one) and the console disables Accept with that text. External mode never blocks — the file is a "copy of <record> · synced <time>" and the decision is written back afterwards. |
| 2.10 | Metrics stay one pure function (`computeMetrics`), now over a declared source model: every metric lists its sources (`git`, `ledger`, `pr`, `ci`, `incidents`, `evals`) and the external feeds arrive as facts (`MetricSources`: PR, CI, incident) rather than being read from adapters. `factsFromRepo` derives the mirror's share purely — PRs from `pr.yaml` (the review job's `review.at` as the first review), CI from per-change runs, `pr.yaml.checks` and `config-pr` suite runs, incidents from `incident.md` and incident-tier triage — and a feed with nothing in it is `null`, which is what makes a metric say "n/a · needs <source>" instead of zero (Q13). |
| 2.10 | GitHub facts are a rebuildable cache (`metric_facts` in the `.sdlc-state` database): reviews per PR head and the combined status per head, fetched on every GitHub sync pass and on demand (`POST /api/metrics/refresh`, `sdlc metrics --refresh`); a merged head is final and fetched once, an open head on every pass. Overlaid on the mirror, a human decision (APPROVED / CHANGES_REQUESTED / DISMISSED) becomes the first review — COMMENTED reviews are what the review job posts and stay with the mirror — and statuses add CI contexts and timing the mirror never saw. The snapshot reports each feed's origin and fetch time (`metricSources`) so a stale cache is visible, never silently current. |
| 2.10 | Trend chips carry the previous window's value and the % change (`previous`, `delta`), computed over the same window length immediately before; ±5% is flat, a previous value of zero gives direction without a %. `breached bands` remains "n/a · needs detection snapshots": detection snapshots have no committed home yet (FR-60 says detection writes them outside the console), so the metric waits for that decision rather than inventing a layout. |
| 2.2 → e2e | Amended after the first run against GitHub (danielyayla/sdlc-website): an artifact branch that carries ledger lines only (the engine records `session started`/`error` on `sdlc/<CHG>/<artifact>` before anything is proposed) opened a "spec.md for review" PR with no spec in it. The sync pass now opens a PR only when the artifact file is on the branch (`docs[i].state !== "absent"`). And a session's later revisions on the branch were never pushed — the PR the reviewer read was stale until the accept pushed and merged in one step — so the pass pushes an open PR's branch whenever the local head moved past `origin/<branch>` (`SyncSummary.pushed`). |
| 2.5 → e2e | The workflows `sdlc init` writes run `npx sdlc` and `pnpm install --frozen-lockfile`; on a repository that has neither a published `sdlc` nor a pnpm lockfile every PR gets a red check. They were removed from the end-to-end target by hand. They stay create-only as designed, but a team without the package on npm must point `bin` at their own install — an `init --sdlc-bin` option is the likely Phase 3 follow-up. |
| 2.1 → e2e | Pushes go through the caller's git credentials, and with `gh auth git-credential` as the helper an exported `GITHUB_TOKEN` takes precedence over the keyring: the token the console reads for the API is also the one git pushes with, so it needs Contents, Pull requests and Commit statuses read/write on the repository. Branch protection on a private repository needs GitHub Pro; the target was made public for the run. |
| 2.1 → e2e | GitHub recomputes a PR's mergeability after every push and reports `mergeable_state: unknown` meanwhile, answering 405 to a merge in that window. Accepting an artifact PR pushes the decision and merges in one step, so the first real accept hit exactly that window and left the decision on the branch with the PR open. `mergePull` now polls the PR until the state is known (10 × 1.5 s) and retries a 405 met while it is still unknown; a 405 with a known state is branch protection's answer and surfaces as before. The fake GitHub models the window (`state.mergeabilityPending`). |
