---
id: CHG-0001
artifact: plan
cycle: 1
spec_sha: 37e1f123191b63108db8f61d4d8614825258d196
rev: 2
accepted_by: dkapper01@gmail.com
accepted_at: 2026-09-09T13:48:42Z
acceptance_line: pnpm build, pnpm test and pnpm lint are green; `grep -rnE '\.chip|"chip|chip[ "]' packages/web/src` prints nothing; render.test.tsx has the 5 named tests (owned card amber/off, single primary-row, dismiss needs a reason, headline numbers agree, removed strings) passing and none of the 21 removed strings; `git log --oneline main..HEAD` shows 24 commits touching only the 10 listed files.
context_manifest: sha256:ca326616d0c8304ddb1860c824a1d8d375e0efadc963e171a8ba66e9aaceaa4e
schema: 1
---
# Plan: Cybercab step 6 — finish Pipeline, Gates, Loop, Security and Metrics to the handoff (from spec.md 37e1f12)

Rewrite of five view components, their CSS and the render test so the views match `sdlc/changes/CHG-0001/design/README.md` line for line (spec R1–R12). Data, routes and callbacks do not change. Every step below is one commit; the commit message is the step's first line. `pnpm test` is green after every commit because each step carries the test edits for the strings it removes.

Decisions this plan makes on the spec's open concerns (none was resolved at gate 2; each is a one-line change if the owner decides otherwise):
- C1/C2: the done signal is the selector grep `grep -rnE '\.chip|"chip|chip[ "]' packages/web/src` → empty. `--radius-chip` (a §6 token, unreferenced) and `record.chip` in `lib/format.ts` (a core field) stay. The tokens.css comment on line 105 is reworded so the literal `grep chip` returns only those two lines.
- C3: `app.tsx` and `lib/format.ts` are in the file list.
- C4: no `it` block is deleted; every dropped assertion is replaced in the same `it` by the handoff replacement string (named per step).
- C5: commit messages are `sdlc(web): <removals-log line>`; additions that are not removals use the same prefix with a plain description.
- C6/C10: follow the handoff — the agent word leaves Pipeline line 1 (edge + pulse carry it; line 3 keeps `c.status`), trends render `▲`/`▼`.
- C7: `docs/decisions.md` gets one appended row (step 24) saying rows 3.4 and 3.6's rendering is superseded; the old rows are not rewritten.
- C8: evidence `<pre>` keeps content, `white-space: pre-wrap`, `max-height: 220px; overflow: auto`; only background and padding go.
- C9: one scoped rule `.security .item:not(.new)::before { box-shadow: none; }`; no new utility class.

Shared predicates (define once, reuse in Pipeline and Gates):
- `owned(c, role)` = `c.valid && (c.gate ? c.gate.ownerRole === role : c.deploy.productionGate?.open === true && c.deploy.productionGate.ownerRoles.includes(role))` — the same predicate as `gateQueues` in `packages/core/src/queues.ts:18-20`.
- `hasOpenGate(c)` = `c.valid && (c.gate !== null || c.deploy.productionGate?.open === true)`.
- `OTHER_ROLES: Record<Role, string>` = `{ po: "the engineer or tech lead", eng: "the product owner or tech lead" }` (new export in `lib/format.ts`).
- Headline number word: `n === 1 ? "1 decision waits" : \`${n} decisions wait\``; same pattern for `signal(s)` and `finding(s)`.
- Class strings are written in JSX exactly as quoted below; React's server renderer emits them verbatim and the tests match them literally.

## Files that change
packages/web/src/views/Pipeline.tsx
packages/web/src/views/Gates.tsx
packages/web/src/views/Loop.tsx
packages/web/src/views/Security.tsx
packages/web/src/views/Metrics.tsx
packages/web/src/tokens.css
packages/web/src/app.tsx
packages/web/src/lib/format.ts
packages/web/test/render.test.tsx
docs/decisions.md

## Order of work
1. `sdlc(web): Pipeline headline and sub-line for the role` — `Pipeline.tsx`: add `role: Role` to `PipelineProps`; `app.tsx:197` passes `role={state.role}` in the same commit (the web build runs `tsc --noEmit`, so a missing required prop breaks it). Compute over the rendered cards (`!c.closed`): `n` = cards where `owned(c, role)`, `k` = cards.length, `a` = cards with `c.agent`, `o` = cards where `hasOpenGate(c) && !owned(c, role)`. Render before the grid, inside `<div class="pipeline-head">`: `<div class="primary">{n} decisions wait on the {ROLE_LABEL[role]}.</div>` (`1 decision waits on …`; `Nothing waits on the {role}.` at 0) then `<div class="mono faint">{k} changes in flight · {a} agents working · {o} waiting on {OTHER_ROLES[role]}</div>`. `lib/format.ts`: add `OTHER_ROLES`. `tokens.css`: `.pipeline-head { margin-bottom: var(--s-6); }` `.pipeline-head .mono { margin-top: 6px; }`. Test: in the Pipeline `it`, add `toContain("decisions wait on the product owner.")` and `toContain("changes in flight ·")`; render `initialState("eng")` and add `toContain("decisions wait on the engineer.")`.
2. `sdlc(web): .pipeline min-width and column chrome → hairline columns` — `Pipeline.tsx`: column becomes `<section class="pcol">` with `<div class="pcol-head mono"><span>{num} {Name}</span><span class="faint">{count}</span></div>` and `<div class="pcol-caption mono faint">{caption}</div>` where captions are a local `STAGE_CAPTIONS = ["intent.md","spec.md","plan.md","evals","PR + findings","incident → intent.md"]` (drop the `ARTIFACT_NAMES` import; it stays in `format.ts` for ChangeDetail). `tokens.css`: replace `.pipeline` with `display: grid; grid-template-columns: repeat(6, minmax(0, 1fr)); gap: var(--s-5);` (no min-width); add `.pcol { border-top: 1px solid var(--border-subtle); padding-top: 10px; }` `.pcol-head { display: flex; justify-content: space-between; color: var(--text-secondary); }` `.pcol-caption { margin-bottom: 16px; }`; delete `.column`, `.column-caption` (`.column-head/.column-num/.column-count` are still used by Metrics; they go in step 21). Test: replace `toContain("commits intent.md")` with `toContain("incident → intent.md")`; add `not.toContain('class="column')`.
3. `sdlc(web): .card raised panel → .edge-lit button, panel only when owned` — `Pipeline.tsx`: card is `<button class="pcard edge-lit {edge}{owned ? " owned" : ""}">` with `edge` = `owned ? "amber" : hasOpenGate(c) ? "off" : c.agent ? "agent pulse" : "off"` (invalid no longer sets a red edge; the word carries it in step 4). `tokens.css`: add `.pcard { display: block; width: 100%; text-align: left; color: inherit; cursor: pointer; background: transparent; border: 0; border-radius: 0; padding: 12px 14px 12px 16px; margin-bottom: 8px; transition: background 0.12s; }` `.pcard.owned { background: var(--bg-panel); }` `.pcard:hover { background: var(--bg-raised); }`; delete `.card`, `.card.edge-lit`, `.card:hover`, `.card-head`, `.card-title`, `.card-status`. Test: `class="card edge-lit amber"` → `class="pcard edge-lit amber owned"`; `class="card edge-lit agent pulse"` → `class="pcard edge-lit agent pulse"`; add `not.toContain('class="card')`.
4. `sdlc(web): .chip agent ⌁ / amber / red → words in line 1; agent word → edge` — `Pipeline.tsx` line 1: `<div class="pline mono faint">{c.id}{risk high ? <span class="amber-text"> · high risk</span>}{!c.valid ? <span class="red-text"> · invalid</span>}</div>`; drop the `agent-text pulse` span and the `riskLabel` import. Line 2: `<div class="ptitle">{c.title}</div>` with `.ptitle { color: var(--text-primary); font-weight: 500; line-height: 1.35; margin: 4px 0; }`. Test: replace `toContain('<span class="agent-text pulse">agent</span>')` with `not.toContain("agent-text pulse")`; add `not.toContain(">routine<")`.
5. `sdlc(web): .env-strip and two-line status → one mono line 3, owner lowercase` — `Pipeline.tsx` line 3, one `<div class="pline mono {owned ? "amber-text" : "faint"}">`: gate → `{c.gate.label} · {c.gate.ownerLabel} · {waitingFor(c.gate.since, now)}` (core's `ownerLabel` is already lowercase; drop the `gateOwnerLabel` import); open production gate → `Deploy to {env} · {ownerRoles.map(r => ROLE_WORD[r]).join(" or ")} · rehearsal pending|waiting {rel}` where `ROLE_WORD` is a local const mapping `po/eng/tech_lead` → `product owner/engineer/tech lead`; otherwise `{c.status}`. When `showEnvs` (same condition as today) append to the same line `<span aria-label="environments">` holding ` · <span class="{tone}" title="…">{glyph} {env}</span>` per environment with the existing `ENV_GLYPH`/`ENV_TONE` (`faint` when not deployed). `tokens.css`: delete `.gate-line`, `.env-strip`. Test: `toContain("TECH LEAD")` → `toContain("· tech lead ·")`; in the Deployment `it` replace `toContain('class="env-strip mono"')` with `toContain('aria-label="environments"')` and keep `toContain("· staging")`; add `not.toContain("env-strip")` to the Pipeline `it`.
6. `sdlc(web): "Nothing here" empty → column has no cards` — `Pipeline.tsx`: delete the `.empty` div (line 28). `.empty` stays in tokens.css (Config, Sessions use it). Test: the Pipeline `it` already has `not.toContain("Nothing here")`; keep it.
7. `sdlc(web): h2.yours heading → headline` — `Gates.tsx`: `n` = length of the yours rows (`queues.yours` ids mapped to changes and filtered by `hasOpenGate`, the filter the rows already apply); render `<div class="primary">{n} decisions wait on the {ROLE_LABEL[role]}.</div>` or `Queue clear — nothing waits on the {role}.`; delete the `h2.section-head` and both `.empty` divs. `tokens.css`: `.gates .primary { margin-bottom: var(--s-6); }`. Test (Gates `it`): `toContain("Yours · product owner")` → `toContain("decisions wait on the product owner.")`; `toContain("Yours · engineer")` → `toContain("decisions wait on the engineer.")`. Add `it("Pipeline and Gates headline numbers agree")`: `const count = (html: string) => { const m = /(\d+) decisions? waits? on the/.exec(html); return m ? Number(m[1]) : 0; }`; for each of po and eng expect `count(board render)` to equal `count(gates render)` (seed: 3 for po, 2 for eng).
8. `sdlc(web): .row border/radius/background → hairline + hover, grid 110/20` — `Gates.tsx` yours row: `<button class="grow edge-lit amber">` with `<span class="mono muted">{id}</span><span class="gbody"><span class="glabel">{gate label or Deploy to {env}}</span><span class="gmeta">{title} · {stage}{" · rollback rehearsal pending" when blocked}</span></span><span class="mono faint">{waitingFor(...)}</span>`. `tokens.css`: `.gates { max-width: 820px; }` `.grow { display: grid; grid-template-columns: 110px minmax(0, 1fr) auto; gap: 20px; align-items: baseline; width: 100%; background: transparent; border: 0; border-top: 1px solid var(--border-subtle); padding: 14px 0 14px 16px; cursor: pointer; color: inherit; text-align: left; font: inherit; transition: background 0.12s; }` `.grow:hover { background: var(--bg-panel); }` `.gbody { display: flex; flex-direction: column; min-width: 0; }` `.glabel { color: var(--text-primary); font-weight: 500; }` `.gmeta { color: var(--text-muted); }`; delete `.gates h2`, `.row`, `.row:hover`, `.row .label`, `.row .meta`, `.row .since` (`.inline-reason .row` stays). Test: add `toContain('class="grow edge-lit amber"')` to the Gates `it`; the Deployment `it` keeps `Deploy to production` and `rollback rehearsal pending`.
9. `sdlc(web): "Other role" heading and opacity → eyebrow "Waiting on …" and colour` — `Gates.tsx`: after the yours rows, `<div class="eyebrow gother-head">Waiting on {OTHER_ROLES[role]} · {m}</div>` (`m` = other rows count) then rows `<button class="grow other edge-lit off">` with `<span class="mono">{id}</span><span>{label} · {title}</span><span class="mono">{waitingFor(...)}</span>`. `tokens.css`: `.grow.other { padding: 12px 0 12px 16px; color: var(--text-muted); }` `.grow.other .mono { color: var(--text-muted); }` `.gother-head { margin: 40px 0 4px; color: var(--text-faint); }`. Test: the four `indexOf("Other role")` comparisons use `"Waiting on the engineer or tech lead"` (po) and `"Waiting on the product owner or tech lead"` (eng); add `not.toContain("Other role")`.
10. `sdlc(web): h2.eyebrow "Triage queue · N" → headline; triage first, bands second` — `Loop.tsx`: headline `<div class="primary">{open.length} signals in the triage queue.</div>` (`1 signal …`; `Queue clear — the loop is feeding itself.` at 0); delete the `view-head`, the `Triage queue` section-head and the `.empty` div; move the whole `<table>` + `.footer` JSX below the `.items` div unchanged (steps 12–13 rewrite it). Test ("Loop shows the bands table…"): add `toContain("2 signals in the triage queue.")`; `not.toContain("Queue clear")` stays; the 3.5 Loop `it` is untouched.
11. `sdlc(web): triage item → 22px title, evidence without background, time right, form 560` — `Loop.tsx` meta row: append `<span class="when">{t.data.createdAt}</span>` (ISO verbatim; no `now` prop, R12); title keeps class `item-title`. `tokens.css`: `.loop .item-title { font-size: var(--text-primary-size); line-height: 1.2; }` `.item-meta .when { margin-left: auto; }` `.item .evidence { margin: 12px 0 0; padding: 0; background: transparent; font-family: var(--font-mono); font-size: var(--text-data); line-height: 1.6; white-space: pre-wrap; color: var(--text-muted); max-height: 220px; overflow: auto; }` (`.inline-reason` already carries `max-width: 560px`). Test: existing `class="item edge-lit amber"` stays; add `toContain('class="when">2026-')`.
12. `sdlc(web): <table> header backgrounds and tr.breached/warned tint → hairline grid rows` — `Loop.tsx`: `<section class="bands"><div class="bands-head mono"><span class="secondary">Bands</span><span>rolling {baselineWindow ?? "30d"} · Western Electric · detection every {detectEvery ?? "15m"} · last {latest ?? "never"}</span><span class="spacer"></span>{onDetect ? <button class="btn text" title="run the detection script now">Run detection</button> : null}</div>` then one `<div class="band-row mono">` per band with six cells: `<span class="secondary">{metric}</span><span class="muted">{fmt(baseline)}</span><span class="{breached ? "amber-text" : s ? "secondary" : "faint"}">{s ? fmt(current) : "no data"}</span><span class="faint">{sigma}σ or —</span><span class="{tier >= 2 ? "amber-text" : "faint"}">{tier}σ or —</span><span class="{breached ? "" : "muted"}">{s?.status ?? "no data · needs detection snapshots"}{triage ids as <span class="mono amber-text word" title="open triage item">}{live job as <span class="mono faint word" title={key}>{kind} {state} · {session}</span>}</span>`; no bands → one `band-row` reading `no bands.yaml`. `tokens.css`: `.bands { margin-top: 48px; }` `.bands-head { display: flex; gap: 14px; align-items: baseline; color: var(--text-muted); padding-bottom: 10px; border-bottom: 1px solid var(--border-subtle); }` `.band-row { display: grid; grid-template-columns: 180px 110px 110px 60px 50px minmax(0, 1fr); gap: 16px; padding: 10px 0; border-top: 1px solid var(--border-subtle); }` `.band-row .word { margin-left: 10px; }`; delete `.bands { width…border-collapse }`, `.bands th`, `.bands td`, `.bands td + td, .bands th + th`, `.bands td.num`, `.bands tr.breached td:first-child`, `.bands tr.warned td:first-child`, `.bands .word`. Test (3.4 `it`): `toContain('class="breached"')` → `toContain('class="amber-text">842 ms')`; `toContain("last snapshot " + ts)` → `toContain("last " + ts)`; everything else stays. Test ("Loop shows the bands table…"): `toContain("rolling 30d baseline · Western Electric rules")` → `toContain("rolling 30d · Western Electric · detection every 15m · last never")`; `toContain("detection every 15m · last snapshot never")` → `not.toContain("<table")`.
13. `sdlc(web): .footer paragraph → section head + one mono line` — `Loop.tsx`: `<div class="foot-line mono">1σ log · 2σ diagnose read-only · 3σ propose via PR or runbook {ids}</div>` where `ids` = runbook ids joined by ` · ` (string entries keep today's `(no command)` suffix: a misconfiguration must explain itself); Run detection now lives in the bands head (step 12). `.footer` is deleted in step 17 when Security stops using it. `tokens.css`: `.foot-line { color: var(--text-faint); margin-top: 10px; }`. Test: `toContain("1σ log, 2σ diagnose read-only, 3σ propose via PR or pre-approved runbook.")` → `toContain("1σ log · 2σ diagnose read-only · 3σ propose via PR or runbook rollback")`; `toContain("runbooks: rollback")` → `not.toContain("runbooks:")`.
14. `sdlc(web): .subhead → headline + sub-line` — `Security.tsx`: `n` = findings with `status === "new" && f.resolved === undefined`; `<div class="primary">{n} findings need a route.</div>` (`1 finding needs a route.`; `No new findings.` at 0). Sub-line `<div class="mono faint view-sub">`: when `latestRun` exists → `{source of the finding that holds latestScan, else "recurring scans"} · {repos} repo(s) · last run {at} · <a>{run id}</a> (or the plain id without url) · {validated} validated`; otherwise the existing sentence unchanged. Delete the `view-head` and the `.empty` div. `tokens.css`: `.loop .primary, .security .primary { margin-bottom: 4px; }` `.view-sub { margin-bottom: var(--s-6); }`. Test (Security `it`): add `toContain("1 finding needs a route.")`; the 3.5 Security `it`: add `toContain("2 findings need a route.")` and keep `last run 2026-09-08T06:04:12Z`.
15. `sdlc(web): every .item a panel → first new finding is the primary row` — `Security.tsx`: `primaryId = findings.find(f => f.status === "new" && f.resolved === undefined)?.id`; class string `item edge-lit {red|amber|off}{new && !resolved ? " new" : ""}{dismissed || resolved ? " dismissed" : ""}{f.id === primaryId ? " primary-row" : ""}` where the colour follows `sev` even when dismissed (`off` only for low). The status word moves to the end of the meta row as `<span class="when {new ? "amber-text" : "muted"}">`; the `resolved by scanner · {at}` span keeps `green-text` and follows it. `tokens.css`: `.security .item { background: transparent; padding: 14px 24px; }` `.security .item .item-title { font-size: var(--text-body); font-weight: 500; }` `.security .item.primary-row { background: var(--bg-panel); padding: 20px 24px; }` `.security .item.primary-row .item-title { font-size: var(--text-primary-size); line-height: 1.2; }` `.item.dismissed { opacity: 0.6; }`. Test: `toContain('class="item edge-lit red"')` → `toContain('class="item edge-lit red new primary-row"')`; add `it("the first new finding is the only primary row")`: render eng security, expect `(html.match(/primary-row/g) ?? []).length` to be 1, and expect the substring from `html.indexOf('primary-row"')` to the next `</article>` to contain `SEC-0118`.
16. `sdlc(web): glow only while new; low → off` — `tokens.css`: `.security .item:not(.new)::before { box-shadow: none; }` (the one scoped rule, C9). No JSX change beyond step 15. Test: in the 3.5 Security `it` add `toContain('class="item edge-lit amber new"')` (SEC-0121, medium, new) and `toContain('class="item edge-lit amber dismissed"')` (SEC-0122, medium, resolved).
17. `sdlc(web): footer three clauses → one mono line` — `Security.tsx`: `<div class="foot-line mono">fixes reach production only through the PR gate · the proposing agent cannot approve its own fix</div>`. `tokens.css`: delete `.footer`, `.footer .btn.text`. Test: `toContain("the proposing agent cannot approve its own fix")` stays; add `not.toContain('class="footer')`.
18. `sdlc(web): .metrics-sources row and "30 days" caption → sub-line` — `Metrics.tsx`: `<div class="primary">Metrics</div><div class="mono faint view-sub msub"><span>30-day window vs the 30 before</span>{FEEDS.map(f => <span class={via === "none" ? "amber-text" : ""}>{label} · {via(...)}</span>)}</div>`; delete the `metrics-sources` block and the `column-count` "30 days" span. `tokens.css`: `.msub { display: flex; gap: 16px; flex-wrap: wrap; }`; delete `.metrics-head, .metrics-sources`, `.metrics-head`, `.metrics-sources`, `.metric-sources`. Test (Metrics `it`): `toContain('<span class="metric-sources">pr</span>')` → `not.toContain("metric-sources")`; keep `PR metadata · git mirror` and `incident records · git mirror`; add `not.toContain("30 days")` and `toContain("30-day window vs the 30 before")`.
19. `sdlc(web): .stage-plane and .halves → 3-column grid of stacked metrics with a kind word` — `Metrics.tsx`: delete `Half`; grid `<div class="mgrid">` with one `<section class="mstage">` per stage: `<div class="mstage-head mono">{num} {Name}</div>` then for `[...s.leading.map(v => ({ v, kind: "leading" })), ...s.lagging.map(v => ({ v, kind: "lagging" }))]` a `<div class="mrow">` with `<div class="mvalue-row"><span class="mvalue tabular">{fmt(v)}</span><Trend v={v} /><span class="mkind mono">{kind}</span></div><div class="mname">{v.name}</div><div class="mono faint">{v.note} · {v.sources.join(" · ")}</div>`. `tokens.css`: `.metrics { max-width: 1200px; display: block; }` `.mgrid { display: grid; grid-template-columns: repeat(3, minmax(0, 1fr)); gap: 40px 48px; }` `.mstage-head { color: var(--text-secondary); padding-bottom: 8px; border-bottom: 1px solid var(--border-subtle); }` `.mrow { padding: 14px 0 4px; }` `.mvalue-row { display: flex; align-items: baseline; gap: 10px; }` `.mvalue { font-size: var(--text-primary-size); line-height: 1.2; font-weight: 500; color: var(--text-primary); }` `.mkind { margin-left: auto; color: var(--text-faintest); }` `.mname { color: var(--text-secondary); }`; delete `.stage-plane`, `.halves`, `.metric`, `.metric-head`, `.metric-name`, `.metric-value`, `.metric-note`. Test: `(html.match(/class="half"/g) ?? []).length` is 12 → `(html.match(/class="mstage"/g) ?? []).length` is 6; add `toContain('class="mkind mono">leading<')`, `not.toContain(">Leading<")`, `not.toContain(">Lagging<")`, `toContain("median of 1 · review job · pr")` (the `review time per PR` metric: note `median of 1 · review job`, source `pr`).
20. `sdlc(web): TrendChip words → ▲/▼ glyph in state colour` — `Metrics.tsx` `Trend`: `trend` null or `flat` → `<span class="trend mono faint" title={fmtPrev(v)}>—</span>`; else glyph `▲` for `up`, `▼` for `down`, text `{glyph}{delta === null ? "" : ` ${delta > 0 ? "+" : ""}${delta}%`}`, class `trend mono green-text` when `v.trend === v.better` else `trend mono amber-text`, `title={fmtPrev(v)}` kept. Test: keep `toContain('title="previous window: 0"')`; replace `toContain('class="trend mono green-text"')` with `toMatch(/class="trend mono green-text" title="[^"]*">[▲▼]/)`.
21. `sdlc(web): .column-head orange numeral → mono grey; delete .column*` — the Metrics head is already mono grey via `.mstage-head` (step 19); this step deletes `.column-head`, `.column-num`, `.column-count` from `tokens.css` after `grep -rn "column-" packages/web/src` shows no hit. Test: add `not.toContain('class="column')` to the Metrics `it`.
22. `sdlc(web): tokens.css sweep — chip comment, unreferenced rules, gateOwnerLabel` — `tokens.css`: reword the comment block at lines 104–106 to `/* Component rules below this line follow the mocks in sdlc/changes/CHG-0001/design: no borders on planes, radii from the tokens above, state as words in state colours or a lit edge. */`; run `for s in $(grep -oE '^\.[a-z-]+' packages/web/src/tokens.css | sort -u); do grep -rq "${s#.}" packages/web/src/views packages/web/src/app.tsx || echo "$s"; done` and delete every selector it prints that this change orphaned (expected: none beyond those already deleted; never delete `.empty`, `.section-head`, `.eyebrow`). `lib/format.ts`: delete `gateOwnerLabel` (no importer left). Proof for this step: `grep -rnE '\.chip|"chip|chip[ "]' packages/web/src` prints nothing; `grep -rn chip packages/web/src` prints only `tokens.css:40` (`--radius-chip`) and `lib/format.ts:86` (`r.chip`).
23. `sdlc(web): tests — owned card edge, Loop dismiss requires a reason, removed strings` — `render.test.tsx`: (1) `it("a Pipeline card lights amber for the role that owns its gate and off for the other")`: helper `card = (html, id) => html.slice(html.lastIndexOf("<button", html.indexOf(id)), html.indexOf("</button>", html.indexOf(id)))`; expect `card(po board, "CHG-0022")` to contain `class="pcard edge-lit amber owned"` and `card(eng board, "CHG-0022")` to contain `class="pcard edge-lit off"`. (2) `it("a Loop dismiss opens an InlineReason whose submit needs a reason and tunes optionally")`: export `DISMISS_TRIAGE_FIELDS` from `Loop.tsx` (the `fields` array literal moved out of the JSX: `reason` required by default, `tune` with `required: false`, placeholders built by a function of the id) and pass it to `InlineReason`; render `{ ...initialState("po"), view: "loop", form: { kind: "dismiss-triage", id: "TRI-0042" } }` and expect `toContain('aria-label="Dismiss · tune band"')`, `toContain('<button type="button" class="btn" disabled="">Dismiss · tune band</button>')`, `toContain("Why TRI-0042 is dismissed — required")`, `toContain("Tune the band? — optional note")`; then `expect(canSubmit(DISMISS_TRIAGE_FIELDS("TRI-0042"), { reason: "", tune: "x" })).toBe(false)` and `expect(canSubmit(DISMISS_TRIAGE_FIELDS("TRI-0042"), { reason: "noise", tune: "" })).toBe(true)` (`canSubmit` imported from `../src/views/InlineReason`). (3) `it("the removed strings are gone from every view for both roles")`: for `["board","gates","loop","security","metrics"]` × `["po","eng"]` render and assert `not.toContain` for each of the 21 strings: `⌁ agent`, `agent-text pulse`, `>routine<`, `>Leading<`, `>Lagging<`, `class="half"`, `Triage queue`, `Yours · product owner`, `Yours · engineer`, `Other role`, `Nothing here`, `commits intent.md`, `TECH LEAD`, `class="card `, `class="breached"`, `class="env-strip mono"`, `<span class="metric-sources">`, `pre-approved runbook`, `rolling 30d baseline · Western Electric rules`, `last snapshot never`, `class="chip`.
24. `sdlc(docs): decisions — Cybercab step 6 supersedes the 3.4 and 3.6 rendering` — `docs/decisions.md`: append one row to the "Decisions made during build" table: `| CHG-0001 | Cybercab step 6 (handoff in sdlc/changes/CHG-0001/design): the bands table with tinted rows (3.4) and the board's env strip and gate strip (3.6) are superseded — bands are a hairline grid whose current value is amber on a breach; a board card carries its state as a lit edge and one mono line 3; every view opens with the decision sentence for the role. Rows 3.4 and 3.6 describe the data, not the rendering, from here on. |`. Nothing else in the file changes.

## Risks
- Class-name assertions depend on attribute order: React's server renderer emits `className` exactly as written, so every class string above is the literal to write in JSX (`pcard edge-lit amber owned`, `grow edge-lit amber`, `item edge-lit red new primary-row`). A reordered template literal fails the test, not the build.
- Pipeline and Gates counts diverge if `owned` drifts from `gateQueues` (core filters `valid` and treats the production gate as a gate only when `c.gate === null`). The equality test in step 7 catches it for the seed; keep the predicate identical to `packages/core/src/queues.ts:18-20`.
- Deleting shared CSS too early: `.column-head/.column-num/.column-count` are used by Metrics until step 19, `.footer` by Security until step 17, `.items/.item*` by both Loop and Security throughout. Grep for the selector before each deletion; `.empty`, `.section-head`, `.eyebrow`, `.inline-reason .row` are used by Config, Sessions, ChangeDetail and InlineReason and stay.
- plan-sync: every commit's paths must be in "Files that change" above; `sdlc/changes/**` ledger writes are exempt. A commit touching any other file is blocked by the hook — that is the signal to come back to this plan, not to widen the commit.
- The intent said "nothing outside packages/web"; step 24 touches `docs/decisions.md` (C7) because the decisions preamble requires superseded rows to be amended in a commit that says why. If the tech lead wants it in a separate change, send this plan back naming step 24 and the file; nothing else depends on it.
- C6 and C10 follow the handoff (agent word gone from line 1, `▲`/`▼` glyphs). If the product owner reverses either, it is one span in step 4 or step 20; the spec's concerns record the choice.
- The Loop time is `t.data.createdAt` verbatim (ISO) to avoid adding a `now` prop to Loop (R12: props unchanged); it reads as data, not prose.
- `--radius-chip` is unreferenced but kept (C2, decision 1.2); the literal `grep chip` therefore never reaches zero. The selector grep in step 22 is the done signal.
- test-freeze does not fire (kind feature, no repro), so edits to `render.test.tsx` are allowed; the C4 rule (replace, never delete an `it`) is enforced by review, not by a hook.

## Proof
- `pnpm build`, `pnpm test`, `pnpm lint` at the tip, output pasted verbatim in the final round; run `pnpm test` before each of the 24 commits so every commit is green.
- `grep -rnE '\.chip|"chip|chip[ "]' packages/web/src` → no output; `grep -rn chip packages/web/src` → exactly the two lines `tokens.css:40` (`--radius-chip`) and `lib/format.ts:86` (`r.chip`).
- `pnpm vitest run packages/web/test/render.test.tsx -t "owns its gate|only primary row|needs a reason|removed strings|headline numbers agree"` → 5 tests pass (the three handoff tests plus the headline-equality and removed-strings tests).
- `git log --oneline main..HEAD` → 24 commits, one per step, messages as written above; `git diff --name-only main...HEAD` lists exactly the ten files in "Files that change".
- No screenshot: CLAUDE.md has no Visual line. To see it, `sdlc serve` on the seed and switch role in the top bar: the Pipeline headline reads `3 decisions wait on the product owner.` then `2 decisions wait on the engineer.`
