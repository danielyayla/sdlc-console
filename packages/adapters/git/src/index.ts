/**
 * @sdlc/adapter-git — thin wrapper over the real `git` CLI.
 * The only component that writes to the repo. Filled in by build-order item 0.7.
 */
export const PACKAGE_NAME = "@sdlc/adapter-git" as const;
